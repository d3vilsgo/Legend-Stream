name: Retired category drawer isolation helper

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "One-off drawer isolation migration completed; workflow retired."
