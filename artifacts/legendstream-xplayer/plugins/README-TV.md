# Android TV generated manifest

`withAndroidTV.js` adds the optional Leanback and touchscreen features, keeps the phone LAUNCHER category, adds LEANBACK_LAUNCHER, sets the application banner, and copies `assets/images/tv-banner.png` to the generated Android drawable resources during Expo prebuild.
